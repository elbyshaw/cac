1. in the root of this repo, run `git submodule update --init --recursive`

2. If on macOS (@niceovens 👀) follow [the macOS installation instructions for librelane](https://librelane.readthedocs.io/en/latest/installation/nix_installation/installation_macos.html).

3. This is all you really need to do for now. Going forward, we'll probably use the command `./nix.sh` in the `sapr/` directory to enter the nix shell, but this hasn't really been configured yet.
